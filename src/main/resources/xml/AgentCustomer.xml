<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.AgentCustomerDao">
    <insert id="AgentCustomerInsert">
        insert into AgentCustomer VALUES(DEFAULT,#{user_id.id},#{contact.id},#{date},#{type},#{state})
    </insert>
</mapper>